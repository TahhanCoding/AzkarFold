---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
import AppStoreButton from '../components/AppStoreButton.astro';
---

<Layout title="Azkar Fold - A beautiful way to stay connected to your daily Azkar">
  <Navigation />
  
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-background"></div>
    <div class="container">
      <div class="hero-content fade-in">
        <div class="hero-text">
          <div class="app-icon">🕌</div>
          <h1>Azkar Fold</h1>
          <p class="tagline">A beautiful way to stay connected to your daily Azkar</p>
          <p class="description">
            Create custom azkar, count any zekr with ease, and stay connected to your daily spiritual practice 
            with Morning and Evening azkar featuring Arabic text, translations, and transliterations.
          </p>
          <div class="hero-actions">
            <AppStoreButton />
            <a href="#features" class="btn btn-secondary">Learn More</a>
          </div>
        </div>
        <div class="hero-image">
          <div class="phone-mockup">
            <div class="phone-frame">
              <div class="phone-screen">
                <div class="placeholder-content">
                  <div class="placeholder-header"></div>
                  <div class="placeholder-items">
                    <div class="placeholder-item"></div>
                    <div class="placeholder-item"></div>
                    <div class="placeholder-item"></div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- App Overview -->
  <section class="app-overview" id="features">
    <div class="container">
      <div class="section-header fade-in">
        <h2>Everything you need for your daily Azkar</h2>
        <p>Simple, beautiful, and thoughtfully designed for your spiritual journey</p>
      </div>
      
      <div class="overview-grid">
        <div class="overview-card fade-in">
          <div class="card-icon">📝</div>
          <h3>Azkary</h3>
          <p>Create and count custom azkar tailored to your personal spiritual needs and preferences.</p>
        </div>
        
        <div class="overview-card fade-in">
          <div class="card-icon">🌅</div>
          <h3>Sunnah</h3>
          <p>Built-in Morning and Evening azkar with Arabic text, English translations, and transliterations.</p>
        </div>
        
        <div class="overview-card fade-in">
          <div class="card-icon">⚙️</div>
          <h3>Settings</h3>
          <p>Clean and simple configuration options to customize your experience exactly how you like it.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Highlights -->
  <section class="features">
    <div class="container">
      <div class="features-content">
        <div class="features-text fade-in">
          <h2>Powerful features for your spiritual practice</h2>
          <div class="features-list">
            <div class="feature-item">
              <div class="feature-icon">✓</div>
              <span>Custom Azkar creation and management</span>
            </div>
            <div class="feature-item">
              <div class="feature-icon">✓</div>
              <span>Easy counting for any zekr</span>
            </div>
            <div class="feature-item">
              <div class="feature-icon">✓</div>
              <span>Morning & Evening azkar collections</span>
            </div>
            <div class="feature-item">
              <div class="feature-icon">✓</div>
              <span>Arabic text with English translations</span>
            </div>
            <div class="feature-item">
              <div class="feature-icon">✓</div>
              <span>Transliteration for easy pronunciation</span>
            </div>
            <div class="feature-item">
              <div class="feature-icon">✓</div>
              <span>Progress saved locally on your device</span>
            </div>
            <div class="feature-item">
              <div class="feature-icon">✓</div>
              <span>Designed for both iPhone & iPad</span>
            </div>
          </div>
        </div>
        <div class="features-visual fade-in">
          <div class="feature-showcase">
            <div class="showcase-item">
              <div class="showcase-icon">📱</div>
              <h4>Universal App</h4>
              <p>Works beautifully on all your Apple devices</p>
            </div>
            <div class="showcase-item">
              <div class="showcase-icon">🔒</div>
              <h4>Private & Secure</h4>
              <p>All data stays on your device</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Screenshots Gallery -->
  <section class="screenshots">
    <div class="container">
      <div class="section-header fade-in">
        <h2>See Azkar Fold in action</h2>
        <p>Beautiful, intuitive design that makes your daily practice a joy</p>
      </div>
      
      <div class="screenshots-grid fade-in">
        <div class="screenshot-item">
          <div class="screenshot-placeholder">
            <div class="placeholder-icon">📝</div>
            <p>Azkary View</p>
          </div>
          <h4>Custom Azkar</h4>
          <p>Create and organize your personal azkar collection</p>
        </div>
        
        <div class="screenshot-item">
          <div class="screenshot-placeholder">
            <div class="placeholder-icon">🌅</div>
            <p>Sunnah Morning</p>
          </div>
          <h4>Morning Azkar</h4>
          <p>Start your day with beautiful morning remembrance</p>
        </div>
        
        <div class="screenshot-item">
          <div class="screenshot-placeholder">
            <div class="placeholder-icon">🌙</div>
            <p>Sunnah Evening</p>
          </div>
          <h4>Evening Azkar</h4>
          <p>End your day with peaceful evening prayers</p>
        </div>
        
        <div class="screenshot-item">
          <div class="screenshot-placeholder">
            <div class="placeholder-icon">🔢</div>
            <p>Zekr Counter</p>
          </div>
          <h4>Easy Counting</h4>
          <p>Simple tap counter for any dhikr practice</p>
        </div>
        
        <div class="screenshot-item">
          <div class="screenshot-placeholder">
            <div class="placeholder-icon">⚙️</div>
            <p>Settings View</p>
          </div>
          <h4>Preferences</h4>
          <p>Customize the app to match your needs</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Call to Action -->
  <section class="cta">
    <div class="container">
      <div class="cta-content fade-in">
        <h2>Start your spiritual journey today</h2>
        <p>Join thousands of Muslims who have made Azkar Fold part of their daily practice</p>
        <AppStoreButton class="cta-button" />
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  /* Hero Section */
  .hero {
    background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 50%, #f8fafc 100%);
    padding: calc(80px + var(--space-12)) 0 var(--space-20);
    position: relative;
    overflow: hidden;
    min-height: 100vh;
    display: flex;
    align-items: center;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23e1f5fe' fill-opacity='0.1'%3E%3Cpath d='M30 30c0-11.046-8.954-20-20-20s-20 8.954-20 20 8.954 20 20 20 20-8.954 20-20zm0 0c0 11.046 8.954 20 20 20s20-8.954 20-20-8.954-20-20-20-20 8.954-20 20z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    opacity: 0.5;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-12);
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .app-icon {
    font-size: 4rem;
    margin-bottom: var(--space-4);
    background: linear-gradient(135deg, var(--color-primary), var(--color-accent));
    border-radius: var(--radius-xl);
    width: 80px;
    height: 80px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-xl);
  }

  .hero-text h1 {
    font-size: 3.5rem;
    margin-bottom: var(--space-4);
    background: linear-gradient(135deg, var(--color-primary), var(--color-accent));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .tagline {
    font-size: 1.5rem;
    color: var(--color-gray-600);
    margin-bottom: var(--space-6);
    font-weight: 500;
  }

  .description {
    font-size: 1.125rem;
    color: var(--color-gray-700);
    margin-bottom: var(--space-8);
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    gap: var(--space-4);
    align-items: center;
  }

  .phone-mockup {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .phone-frame {
    width: 280px;
    height: 560px;
    background: linear-gradient(145deg, #2a2a2a, #1a1a1a);
    border-radius: 40px;
    padding: 20px;
    box-shadow: var(--shadow-xl);
    position: relative;
  }

  .phone-screen {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, var(--color-primary-light), var(--color-secondary));
    border-radius: 30px;
    padding: var(--space-6);
    display: flex;
    flex-direction: column;
  }

  .placeholder-content {
    color: white;
    text-align: center;
  }

  .placeholder-header {
    height: 40px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: var(--radius-lg);
    margin-bottom: var(--space-6);
  }

  .placeholder-items {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .placeholder-item {
    height: 60px;
    background: rgba(255, 255, 255, 0.15);
    border-radius: var(--radius-lg);
  }

  /* App Overview */
  .app-overview {
    padding: var(--space-20) 0;
    background-color: var(--color-white);
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-16);
  }

  .section-header h2 {
    margin-bottom: var(--space-4);
  }

  .section-header p {
    font-size: 1.125rem;
    color: var(--color-gray-600);
    max-width: 600px;
    margin: 0 auto;
  }

  .overview-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-8);
  }

  .overview-card {
    background: var(--color-white);
    padding: var(--space-8);
    border-radius: var(--radius-xl);
    text-align: center;
    box-shadow: var(--shadow-md);
    border: 1px solid var(--color-gray-100);
    transition: all var(--transition-normal);
  }

  .overview-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }

  .card-icon {
    font-size: 3rem;
    margin-bottom: var(--space-4);
    background: linear-gradient(135deg, var(--color-primary-light), var(--color-accent));
    border-radius: var(--radius-lg);
    width: 80px;
    height: 80px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto var(--space-4);
  }

  .overview-card h3 {
    margin-bottom: var(--space-3);
  }

  .overview-card p {
    color: var(--color-gray-600);
    margin: 0;
  }

  /* Features */
  .features {
    padding: var(--space-20) 0;
    background-color: var(--color-gray-50);
  }

  .features-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-12);
    align-items: center;
  }

  .features-text h2 {
    margin-bottom: var(--space-8);
  }

  .features-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .feature-item {
    display: flex;
    align-items: center;
    gap: var(--space-3);
  }

  .feature-icon {
    background: var(--color-success);
    color: var(--color-white);
    width: 24px;
    height: 24px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.875rem;
    font-weight: 600;
    flex-shrink: 0;
  }

  .feature-showcase {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .showcase-item {
    background: var(--color-white);
    padding: var(--space-6);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
    text-align: center;
  }

  .showcase-icon {
    font-size: 2.5rem;
    margin-bottom: var(--space-3);
  }

  .showcase-item h4 {
    margin-bottom: var(--space-2);
  }

  .showcase-item p {
    color: var(--color-gray-600);
    margin: 0;
  }

  /* Screenshots */
  .screenshots {
    padding: var(--space-20) 0;
    background-color: var(--color-white);
  }

  .screenshots-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--space-8);
  }

  .screenshot-item {
    text-align: center;
  }

  .screenshot-placeholder {
    background: linear-gradient(135deg, var(--color-gray-100), var(--color-gray-200));
    border-radius: var(--radius-lg);
    padding: var(--space-12) var(--space-6);
    margin-bottom: var(--space-4);
    min-height: 300px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-3);
    border: 2px dashed var(--color-gray-300);
  }

  .placeholder-icon {
    font-size: 3rem;
    opacity: 0.7;
  }

  .screenshot-placeholder p {
    color: var(--color-gray-500);
    font-weight: 500;
    margin: 0;
  }

  .screenshot-item h4 {
    margin-bottom: var(--space-2);
  }

  .screenshot-item p {
    color: var(--color-gray-600);
    font-size: 0.875rem;
    margin: 0;
  }

  /* CTA */
  .cta {
    padding: var(--space-20) 0;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
    color: var(--color-white);
  }

  .cta-content {
    text-align: center;
  }

  .cta h2 {
    color: var(--color-white);
    margin-bottom: var(--space-4);
  }

  .cta p {
    font-size: 1.125rem;
    margin-bottom: var(--space-8);
    opacity: 0.9;
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .hero {
      padding: calc(80px + var(--space-8)) 0 var(--space-12);
      min-height: auto;
    }

    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
      gap: var(--space-8);
    }

    .hero-text h1 {
      font-size: 2.5rem;
    }

    .tagline {
      font-size: 1.25rem;
    }

    .hero-actions {
      justify-content: center;
      flex-wrap: wrap;
    }

    .phone-frame {
      width: 240px;
      height: 480px;
    }

    .features-content {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }

    .overview-grid {
      grid-template-columns: 1fr;
    }

    .screenshots-grid {
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    }

    .hero-actions {
      flex-direction: column;
      align-items: center;
    }
  }

  @media (max-width: 480px) {
    .hero-actions {
      width: 100%;
    }
    
    .hero-actions .btn,
    .hero-actions .app-store-button {
      width: 100%;
      justify-content: center;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    // Add fade-in animation on scroll
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.animationDelay = '0.1s';
          entry.target.classList.add('fade-in');
        }
      });
    }, observerOptions);

    document.querySelectorAll('.fade-in').forEach(el => {
      observer.observe(el);
    });
  });
</script>
</Layout>